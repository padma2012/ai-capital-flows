# Required for the daily data pipeline to extract deal data from articles
OPENAI_API_KEY=sk-...

# Protects the /api/cron/daily endpoint from unauthorized calls
# Generate any random string: openssl rand -base64 32
CRON_SECRET=your-random-secret-here

NODE_ENV=production
